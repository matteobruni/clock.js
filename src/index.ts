export * from "./clock";
export * from "./types";

/*
const clock = new Clock();

document.body.append(clock.canvas);

function updateClock() {
  clock.draw(new Date());

  requestAnimationFrame(updateClock);
}

requestAnimationFrame(updateClock);
*/
